import { TenderlySettings } from 'src/types';

export const mockTenderlySettings: TenderlySettings = {
  tenderlyKey: 'existing-key',
  tenderlyProject: 'existing-project',
  tenderlyUsername: 'existing-username',
};
